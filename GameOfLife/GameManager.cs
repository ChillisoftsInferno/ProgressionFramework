namespace GameOfLife;

public class GameManager
{
    public Map Map { get; set; }
    
    public string SetCells()
    {
        for (int y = 0; y < Map.Height; y++)
        {
            for (int x = 0; x < Map.Height; x++)
            {
                Map.Cells[]
            }
        }
    }
}
